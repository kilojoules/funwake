# Ephemeral / regenerable — not committed
__pycache__/
*.pyc
*.pyo
# content-addressed result caches (regenerable; keyed by schedule hash)
state/*/cache/
# exploration: regenerable materialized scopes + temp candidate
state/*/scope/
state/*/_cand.py
